//
// API tests

import assert from "assert";
import api from "../sources/core/api"

describe('Api Tests', function() {
	describe('#checkUpdates', function() {
		beforeEach(function() {
			return new Promise(function (resolve) {
			    assert.ok(true);
			    resolve();
			  })
    		.then( return true; );
  		});

		it('should return number of cards', function() {
			
		});
	});
})